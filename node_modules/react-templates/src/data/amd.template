define(<%= name ? '\"'+name + '\", ' : '' %>[<%= requirePaths %>], function (<%= requireNames %>) {
    'use strict';
    <%= injectedFunctions %>
    return function() {
        return <%= body %>
    };
});